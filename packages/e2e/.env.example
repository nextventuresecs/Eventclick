# Database configuration (should match your local DB or test DB)
DATABASE_URL=postgresql://Eventclick_admin:1234@localhost:5433/Eventclick_db

# Passwords for the E2E setup script to create test users
PLAYWRIGHT_ADMIN_PASSWORD="local_admin_password_123!"
PLAYWRIGHT_VOLUNTEER_PASSWORD="local_volunteer_password_123!"

# Optional overrides if your local dev server is running on different ports
# PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000
# PLAYWRIGHT_API_BASE_URL=http://localhost:4000
