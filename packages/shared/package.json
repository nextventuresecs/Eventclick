{
  "name": "@application/shared",
  "version": "1.0.0",
  "description": "Shared types, DTOs, and zod schemas for Evently",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(lint not configured yet)\" && exit 0",
    "build": "echo \"(no build — sources are consumed directly)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "zod": "^4.3.6"
  }
}
